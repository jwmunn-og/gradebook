<br />
<div class="row">
  <div class="medium-6 large-offset-3 columns">
    <div class="card">
      <div class="card-section">
        <h4>Enrollment Edit Page</h4>
        <%= simple_form_for @enrollment, :url => course_enrollment_path do |f| %>
          <%= f.input :grade %>
          <br />
          <%= f.submit 'Update Grade', class: 'button' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
